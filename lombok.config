# Instruct Lombok to annotate all generated methods with @lombok.Generated.
# JaCoCo automatically excludes methods annotated with @Generated, which prevents
# Lombok-generated boilerplate (getters, setters, builders, constructors) from
# distorting the code-coverage report.
lombok.addLombokGeneratedAnnotation = true
